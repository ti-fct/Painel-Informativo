<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Painel Informativo FCT/UFG</title>
    <link rel="stylesheet" href="/css/layout-c.css">
</head>

<!-- O atributo 'data-layout' informa ao JS para ativar a lógica dinâmica -->
<body data-screen-id="<%= screenId %>" data-layout="dynamic">
    <header class="main-header">
        <h1>Painel Informativo FCT/UFG</h1>
    </header>

    <main class="content-container">
        <div id="empty-state-container" style="display: none;">
            <h2>Nenhum conteúdo configurado para esta tela.</h2>
        </div>
        <div class="carousel-wrapper">
            <!-- A estrutura do layout-a é genérica e contém todos os elementos necessários.
                 O CSS irá mostrar/esconder o que for preciso para cada modo. -->
            <div class="carousel-item-content">
                <div class="image-container">
                    <img id="carousel-image" src="" alt="Imagem do conteúdo">
                    <div class="image-placeholder">Carregando...</div>
                </div>
                <div class="text-container">
                    <h2 id="carousel-title">Carregando conteúdo...</h2>
                    <p id="carousel-date"></p>
                    <div class="description-scroll-area">
                        <div id="carousel-description"></div>
                    </div>
                    <div class="qr-code-container">
                        <div id="qr-code"></div>
                    </div>
                </div>
            </div>

            <div class="progress-bar-container">
                <div class="progress-bar-fill" id="progress-bar"></div>
            </div>
        </div>
    </main>

    <!-- Passando dados do servidor para o JavaScript do cliente -->
    <script>
    const initialData = <%- JSON.stringify({ screenId, screenName, content, config }) %>;
    </script>

    <!-- Bibliotecas e nosso script principal -->
    <script src="/js/qrcode.min.js"></script>
    <script src="/js/display.js"></script>
</body>

</html>